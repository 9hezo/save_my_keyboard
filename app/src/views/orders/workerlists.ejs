<script src="/js/users/admin.js" defer></script>
<% if (!locals.userInfo) { %>
  <script>
    alert('로그인이 필요한 기능입니다.');
    location.href='/'
  </script>
<% } else if (locals.userInfo.admin) {%>
  <script>
    alert('접근 권한이 없습니다.');
    location.href='/'
  </script>
<% } %>

<div class="container">
  <!-- 현재 요청 상태 -->
  <div id="orders_status_zeroToThree">
    <div class="d-grid gap-2">
      <button class="btn btn-dark" type="button">진행중인 요청</button>
    </div>
      <div class="card border-secondary border-2 mb-1">
      <div class="row">
        <div class="ps-4 pt-2 col-2">
          <img id="order_img" class="img-fluid rounded-start" src="https://placeimg.com/100/100/any" style="width: 100px;">
        </div>
        <div class="p-2 col-8">
          <div class="card-body">
            <h5 id="order_kinds" class="card-title"></h5>
            <p id="order_details" class="card-text"></p>
          </div>
        </div>
        <div class="p-1 col-2">
          <button id="order_status" class="btn btn-success mb-2" style="width: 100px;"></button>
          <!-- <button id="" class="btn btn-outline-danger" style="width: 100px;">취소하기</button> -->
        </div>
      </div>
    </div>
  </div>
  <!-- 현재 요청 끝 -->

  <!-- 신청 히스토리 시작 -->
  <div>
    <div class="d-grid gap-2">
      <button class="btn btn-dark" type="button">신청 히스토리</button>
    </div>

    <!-- 신청 히스토리 -->
    <div id="orders_status_end"></div>
    <!-- 신청 히스토리 끝 -->

  </div>
  <!-- 신청 히스토리 끝 -->
</div>


    <script type="text/javascript">
      const data = <%- JSON.stringify(data) %>;
      console.log('data',data);
      for (let i = 0; i < data.length; i++) {
        let date = new Date(data[i].pickup).toLocaleDateString();
        let status_str = data[i].status;
        if (status_str === 0) {
          status_str = "대기 중";
        } else if (status_str === 1) {
          status_str = "수거 중";
        } else if (status_str === 2) {
          status_str = "수거 완료";
        } else if (status_str === 3) {
          status_str = "배송 중";
        } else if (status_str === 4) {
          status_str = "배송 완료";
        }

        let temp_html = `<tr>
                            <td>${data[i].imageUrl}</td>
                            <td>${status_str}</td>
                            <td>
                              <button type="button" class="btn btn-outline-success">상태 변경</button>
                            </td>
                        </tr>`;
        $('#worklists').append(temp_html);
      }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>

  </body>
</html>
